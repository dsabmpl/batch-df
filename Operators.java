public class Operators {
    public static void main(String[] args) {
        // + - * / %
        // > < >= <= == !=
        // && || !
        // += -= *= /=
        byte a = 10;
        byte b = 20;
        b += a; // b = b + a
        byte c = (byte) (a + b);

        // Conditional Statements
        // if else, switch case
        // multiple if else
        // nested if

        // find out the greater in 3 numbers with only nested if
        // if(true){

        // }
        // else{
        // // false
        // }
    }
}
